require 'spec_helper'

describe SpotifyNewReleases do
  it 'has a version number' do
    expect(SpotifyNewReleases::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
