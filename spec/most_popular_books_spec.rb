require 'spec_helper'

describe MostPopularBooks do
  it 'has a version number' do
    expect(MostPopularBooks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
